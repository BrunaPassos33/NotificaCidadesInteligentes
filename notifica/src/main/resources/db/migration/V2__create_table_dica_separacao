CREATE SEQUENCE dica-separacao_seq START WITH 1 INCREMENT BY 1;

CREATE TABLE TBL_DICA-SEPARACAO (
    id BIGINT PRIMARY KEY,
    tipo_residuo VARCHAR(255) NOT NULL,
    instrucoes TEXT NOT NULL
);