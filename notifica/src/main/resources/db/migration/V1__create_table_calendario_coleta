CREATE SEQUENCE calendario_coleta_seq START WITH 1 INCREMENT BY 1;

CREATE TABLE TBL_CALENDARIO_COLETA (
    id BIGINT PRIMARY KEY,
    data_coleta DATE NOT NULL,
    regiao VARCHAR(255) NOT NULL
);